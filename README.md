# actions-testing

## Warm Tips when testing local actions
1. Call them with `./actions/<action_name>`
1. CHECK OUT THE REPO or the action won't be there!
    ```
    - id: checkout
      uses: actions/checkout@v4
    ```